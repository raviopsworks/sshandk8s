apiVersion: apps/v1
kind: Deployment
metadata:
  name: weatherapp-deployment
  labels:
    app: weatherapp
spec:
  replicas: 100
  selector:
    matchLabels:
      app: weatherapp
  template:
    metadata:
      labels:
        app: weatherapp
    spec:
      containers:
      - name: weatherapp
        image: weatherapp:1.0.0
        env:
          - name: APP_PORT
            value: 8080
          - name: APPID
            valueFrom:
            secretKeyRef:
              name: appidsecret
              key: APPID
        ports:
        - containerPort: 8080
          name: weather-port
      livenessProbe:
        httpGet:
          path: /minneapolis
          port: weather-port
        initialDelaySeconds: 6
        periodSeconds: 6
      readinessProbe:
        tcpSocket:
          port: weather-port
        initialDelaySeconds: 6
        periodSeconds: 6
        
